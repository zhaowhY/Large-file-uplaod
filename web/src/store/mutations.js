export const SET_ROUTES = (state, data) => {
  state.routes = data;
};

export const SET_ROLES = (state, data) => {
  state.roles = data;
};

export const SET_USER = (state, data) => {
  state.user = data;
};
export const SET_EL_OPEN_MENU = (state, data) => {
  state.elOpenMenu = data;
};

export const SET_SUB_TITLE = (state, data) => {
  state.subTitle = data;
};
